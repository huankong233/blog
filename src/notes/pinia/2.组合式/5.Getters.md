---
title: Getters
createTime: 2025/07/17 14:59:30
permalink: /notes/pinia/组合式/getters/
---

```ts title="store.ts"
export const useMainStore = defineStore('main', () => {
  const count = ref(0)

  // 这里直接不演了, 直接用计算属性就可以!
  const moreCount = computed(() => {
    return count.value + 10
  })

  return { count, moreCount }
})
```
